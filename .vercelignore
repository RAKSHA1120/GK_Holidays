# Exclude server.js from Vercel deployment so Vercel treats this project as static files + Serverless Functions
server.js

# Exclude internal scripts and migration tools
scripts/

# Exclude local JSON fallback databases from deployment
*-db.json

# Exclude local environment files
.env
.env.*
